{
  "routes": [
     {
      "route": "/volunteer/admin*",
      "rewrite": "/index.html"
    },
    {
      "route": "/*"
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": ["/*.{ico,js,map,css}"]
  }
}
