{
  "routes": [
     {
      "route": "/calendar/auth/callback*",
      "rewrite": "/index.html"
    },
    {
      "route": "/*"
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": ["/*.{ico,js,map,css}"]
  }
}
